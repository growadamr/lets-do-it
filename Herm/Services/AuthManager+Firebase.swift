import Foundation
import FirebaseAuth

// Configure Firebase
func configureFirebase() {
    // This will be called from AppDelegate or SceneDelegate
    // FirebaseApp.configure() - only if not already configured
}
